import 'package:flutter/material.dart';

class EvenementViewDesktop extends StatelessWidget {
  const EvenementViewDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
